import os
import numpy as np
import sys
import time

cmd="../f/pepo/pepo -u 1 -k -o 208 -w 4 -n 32"
retvalue = os.popen(cmd).readlines()
time.sleep(0.8)
cmd="../f/pepo/pepo -u 1 -k -o 208 -w 6 -n 32"
retvalue = os.popen(cmd).readlines()
time.sleep(0.8)
cmd="../f/pepo/pepo -u 1 -k -o 208 -w 7 -n 32"
retvalue = os.popen(cmd).readlines()
time.sleep(0.8)
cmd="../f/pepo/pepo -u 1 -k -o 310 -r -n 32"
retvalue = os.popen(cmd).readlines()

print retvalue
